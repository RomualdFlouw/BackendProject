@model IEnumerable<CinemaDemoModels.Models.Cinema>

@{
    ViewData["Title"] = "Cinemas";
}

<h1>Cinemas</h1>

<ul>
    @foreach (var item in Model)
    {
        <li><a asp-action="Movies" asp-route-id="@item.Id">@item.Name</a> </li>


    }
</ul>

